import React from 'react'
//still a work in progress

//might delete this later on since header.tsx accomplishes the same task
function NavBar() {
  return (
    <div>NavBar</div>
  )
}

export default NavBar